# Mistral API key (mandatory)
MISTRAL_API_KEY=

# Basic Auth — défini pour activer l'authentification famille.
# Si une seule des deux variables est vide, l'auth est désactivée.
BASIC_AUTH_USER=famille
BASIC_AUTH_PASSWORD=
